query CollectionOverviewQuery($collectionUri: String!) {
    firstInstitution {
        name
        rights {
            text
        }
        uri
    }
    collectionByUri(uri: $collectionUri) {
        description
        name
        objects {
            description
            title
            thumbnail {
                url
            }
            uri
        }
    }
}
